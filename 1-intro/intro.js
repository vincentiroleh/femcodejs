/*
    Our Class target for today:
    * The diff btw:
        var
        let
        const

    * Prompt

    */

    // Var
    // var name = "Vincent";
    // var name = "Precious";
    // var name = "Anita"

    // // Let
    // let full_name = "Vincent Iroleh";
    // full_name = "Precious Ezema";

    // // const
    // const age = 19;
    // age = 20;

    // alert(age);

    // let name = prompt("What is your name?");
    // let age = prompt("How old are you?");

    // alert("Hello, " + name + " you are " + age + " years old");

    // Calculator that adds two numbers entered by the user together, and give them the answer
    let num1 = prompt("Enter first number");
    let num2 = prompt("Enter second number");

    let sum = num1++ + num2++;
    alert(sum)

    // Assessment
    //  Create a simple HTML page about your self
    // Find other ways of converting strings to numbers and converting numbers to string
    // Blog post about the following: variables, difference btw the var, let, and const, alert, prompt
